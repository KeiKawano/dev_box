# dev_box
dev box for rails

```
git clone 

vagrant up
vagrant ssh

bundle install

bundle exec rails s -b 0.0.0.0
```


### FileUp

* bootstrap
* image file upload（carrierwave使用）
* image file upload後のサムネイル表示
